import React from 'react'
import { Folder } from "../../types";

type FolderDetail = Folder
type Folders = Folder[]

const GetFolderList = () => {
    return (
        <div>GetFolderList</div>
    )
}

export default GetFolderList